# 章节优化文本对比功能

## 功能概述

在章节优化功能中添加了文本对比高亮显示，让用户清楚看到优化前后的变化。

## 实现内容

### 1. 新增组件

**TextDiffViewer.vue** - 文本对比查看器
- 支持两种视图模式：
  - **行内对比**：在同一文本中高亮显示变化
  - **并排对比**：左右对比原文和优化后的文本
- 高亮样式：
  - 删除的文字：红色背景 + 删除线
  - 新增的文字：绿色背景
- 统计信息：显示删除、新增、未变的字数

### 2. 更新组件

**ChapterContent.vue** - 章节内容组件
- 集成 `TextDiffViewer` 组件
- 优化结果弹窗改为显示对比视图
- 保存原始内容用于对比

**LayeredOptimizer.vue** - 分层优化器组件
- 添加对比视图支持
- 新增 `showDiff` 方法用于显示对比
- 优化后显示应用/放弃按钮

### 3. 依赖库

- `diff-match-patch` - 文本对比算法库
- `@types/diff-match-patch` - TypeScript 类型定义

## 使用方式

1. 用户点击"分层优化"按钮
2. 选择优化维度（对话/环境/心理/节奏）
3. 可选填写额外优化指令
4. 点击"开始优化"
5. 优化完成后自动显示对比视图
6. 用户可以：
   - 切换"行内对比"和"并排对比"视图
   - 查看统计信息（删除/新增/未变字数）
   - 点击"应用优化"保存修改
   - 点击"取消"放弃修改

## 技术细节

### Diff 算法

使用 Google 的 `diff-match-patch` 库：
- 高效的文本对比算法
- 支持语义清理（semantic cleanup）
- 返回三种操作：删除(-1)、未变(0)、新增(1)

### 高亮渲染

通过 `v-html` 渲染带��式的 HTML：
```javascript
// 删除
<span class="diff-deleted">删除的文字</span>

// 新增
<span class="diff-added">新增的文字</span>

// 未变
普通文字
```

### 安全性

- 使用 `escapeHtml` 函数转义用户输入
- 防止 XSS 攻击

## 文件清单

```
frontend/src/components/
├── TextDiffViewer.vue          # 新增：文本对比组件
├── LayeredOptimizer.vue        # 更新：支持对比视图
└── writing-desk/workspace/
    └── ChapterContent.vue      # 更新：集成对比功能

frontend/package.json           # 更新：添加依赖
```

## 效果预览

### 行内对比模式
- 在同一文本中显示所有变化
- 删除的文字显示为红色删除线
- 新增的文字显示为绿色背景

### 并排对比模式
- 左侧显示原文（含删除标记）
- 右侧显示优化后（含新增标记）
- 方便逐句对比

## 后续优化建议

1. 添加"逐句对比"模式，按段落分组显示
2. 支持导出对比报告（PDF/Word）
3. 添加"接受部分修改"功能
4. 优化大文本性能（虚拟滚动）
5. 添加快捷键支持（如 Ctrl+Enter 应用）
